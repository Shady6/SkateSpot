<% include ./partials/header.ejs %>

<div class="container">
	<div class="row">
		<div class="text-center col mb-2">
			<h2>Profil użytkownika</h2>
		</div>
	</div>

	<div class="row">
		<div class="col text-center"><p>Twoja nazwa wyświetlana - <% if (!!currentUser.google.username) { %>

		<%= currentUser.google.username %>

 		<% } else if (!!currentUser.local.username) { %>

		<%= currentUser.local.username %>
		
		<%	} else if (!!currentUser.facebook.username) { %>

		<%= currentUser.facebook.username %>
		
		<% } %></p></div>
	</div>
</div>


<% include ./partials/footer.ejs %>